/// How the modal surface is presented.
enum ModalLayoutMode {
  /// Bottom sheet sliding up from the bottom edge.
  /// Used in compact layout.
  sheet,

  /// Centered dialog.
  /// Used in expanded layout.
  dialog,
}
